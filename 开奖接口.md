# API 基础接口

https://yu28.top

# Key 密钥

yu28_c4aaa4ccc91a5bf8

# 开奖

kj.json 如 https://yu28.top/api/kj.json?nbr=1&key=yu28_96080e9961a17a9b

1. API 基础
BASE：https://yu28.top
鉴权：X-Api-Key / Bearer（请求时携带，例 ?key=yu28_c4aaa4ccc91a5bf8）
数据格式：JSON（可改 .xml）
限流：个人 Key 不限流
2. 核心接口
GET /api/kj.json?nbr=N
返回最近 N 期开奖，最新在前
nbr：1-100 的正整数（条数，不是期号），省略为 1响应结构：
{
  "countdown": "02:40",
  "data": [
    {
      "nbr": "3463701", // 期号（字符串）
      "time": "2026-07-31 11:25:00",       // 开奖时间（北京时间）
      "number": "4+8+2=14",                // 三位和式
      "combination": "小双"                // 大小单双组合
 }
  ]
}
####字段说明

字段	含义
countdown
相对最新一期的下期倒计时（MM:SS），未知可能为空串
data[].nbr
期号（字符串）
data[].time
开奖时间（北京时间）
data[].number
形如 4+8+2=14
data[].combination
形如 小双（大小单双组合文案）
3. 主循环流程（拉取+推送）

进程启动
  → 拉 API 拿倒计时
  → 解析倒计时为秒数 countdown_sec
  → 首次启动则把 last_issue 初始化为当前期号
  → sleep(countdown_sec + 1)
  → sleep(3)
  → 拉 API 拿当前期号，更新 _current_nbr
  → 拉 API 拿最新结果（fetch_and_push 内部第三次）
  → 去重判断：nbr == last_issue ?
 → 是则跳过推送，返回主循环
  → 否则生成文本消息
  → 串行推送文本消息
  → 更新 last_issue 为新期号
  → push_count 加 1
  → 写盘 push_count.json
  → 回到主循环顶端
  → 下一轮重新拉 API 拿倒计时
  → 循环往复